# Update Progress Modal Implementation

## Tasks
- [x] Add update progress modal to admin.html with progress bar, timer, and warning message
- [x] Modify admin.js to show modal on "Proceed with Update", poll /api/update-status every second, update progress/timer, hide modal on completion
- [x] Add update progress modal to employee.html with similar elements
- [x] Add script to employee.html to poll /api/update-status every 5 seconds and show/hide modal accordingly
- [x] Test modal display and progress updates
- [x] Adjust timing if needed
